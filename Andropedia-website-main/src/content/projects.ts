import type { PublicProject } from "@/lib/public-content";

// Publish only verified club work. Empty data intentionally produces an honest
// empty state when the Projects redesign is implemented in Phase 7.
export const publicProjects: readonly PublicProject[] = [];
